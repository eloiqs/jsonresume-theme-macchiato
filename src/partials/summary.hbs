{{#resume.basics}}
    {{#if summary}}
        <div class="container summary-container">
            {{> title value="Summary"}}
            {{#each summary}}
            <p class="summary">
                {{this}}
            </p>
            {{/each}}
        </div>
    {{/if}}
{{/resume.basics}}
